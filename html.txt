<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<title>Toyota Notepad Design</title>
<style>
  body {
    margin: 0;
    background: linear-gradient(180deg, #1a1a1a 0%, #2e2e2e 100%);
    font-family: Arial, sans-serif;
    color: white;
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 100vh;
  }

  .card {
    background: #111;
    border-radius: 20px;
    padding: 30px;
    box-shadow: 0 8px 30px rgba(0,0,0,0.6);
    text-align: center;
    width: 600px;
  }

  .logo {
    font-size: 42px;
    font-weight: bold;
    color: #ff1b1b;
    letter-spacing: 2px;
    margin-bottom: 20px;
  }

  /* Toyota car shape (simple art) */
  .car {
    margin: 0 auto;
    width: 300px;
    height: 120px;
    position: relative;
    background: #e50914;
    border-radius: 60px 60px 20px 20px;
    box-shadow: inset 0 0 20px rgba(255,255,255,0.3);
  }

  .car::before {
    content: "";
    position: absolute;
    top: -40px;
    left: 40px;
    width: 220px;
    height: 80px;
    background: #c3070f;
    border-radius: 120px 120px 0 0;
    box-shadow: inset 0 0 20px rgba(255,255,255,0.3);
  }

  .wheel {
    position: absolute;
    bottom: -30px;
    width: 70px;
    height: 70px;
    background: black;
    border-radius: 50%;
    border: 6px solid #ccc;
  }

  .wheel.left { left: 25px; }
  .wheel.right { right: 25px; }

  .light {
    position: absolute;
    top: 20px;
    width: 30px;
    height: 20px;
    background: yellow;
    border-radius: 4px;
  }
  .light.left { left: -10px; }
  .light.right { right: -10px; }

  .btn {
    margin-top: 25px;
    padding: 12px 22px;
    font-size: 16px;
    font-weight: bold;
    border-radius: 10px;
    border: none;
    cursor: pointer;
    background: #ff1b1b;
    color: white;
    transition: 0.3s;
  }
  .btn:hover { background: #e60000; }
</style>
</head>
<body>
  <div class="card">
    <div class="logo">TOYOTA</div>
    <div class="car">
      <div class="wheel left"></div>
      <div class="wheel right"></div>
      <div class="light left"></div>
      <div class="light right"></div>
    </div>
    <button class="btn" onclick="alert('Drive Your Toyota Forward! 🚗💨')">Start Engine</button>
  </div>
</body>
</html>
